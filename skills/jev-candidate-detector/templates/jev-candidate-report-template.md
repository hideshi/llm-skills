# Jev 適用候補 & 期待TCO試算レポート

## 1. エグゼクティブサマリー
- **分析対象ドキュメント**: [例: requirements.md, docs/domain/domain-model.md]
- **調査日 / 価格基準日**: [YYYY-MM-DD]（価格は変動するため、レポート作成時に一次情報を再確認すること）
- **検出候補サマリー**: 検出 X 件（うち Eligible ★4以上: Y 件）
- **ADR要否判定**: [全社基盤ADRが必要 / 機能別ADRが必要 / design.md内の記録で十分]（判定基準は SKILL.md ワークフロー Step 5 を参照）

---

## 2. 検出された Jev 適用候補一覧 (フェーズ間契約)

<!-- 以下の候補 #1 / #2 は記入例。実際の出力では分析結果に置き換えること -->

| 候補ID | Requirement ID / 原文位置 | 業務判断の責務 | 採用プリミティブ | 適格性 & 適合度 | 承認ステータス (decision) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **#1** | `REQ-SEC-01` (`docs/design/kid-safe.md` §Kid-Safe判定) | 入力テキストのKid-Safe判定 | **noul** (Yes/No確率) | Eligible ★★★★★ | `proposed` (承認待ち) |
| **#2** | `REQ-ROUTE-03` (`requirements.md` §問い合わせトリアージ) | 問い合わせの担当部署トリアージ | **choice** (5択) | Eligible ★★★★☆ | `proposed` (承認待ち) |

---

## 3. 各候補の詳細分析 & 期待TCO (総保有コスト) 試算

### 候補 #1: [候補名]
- **Requirement ID & 原文**: `[REQ-ID]` 「...」
- **現在の判定手段**: [例: 正規表現ルール / 生成LLM (GPT-4o-mini) / 人手]
- **採用プリミティブ**: [choice / score / noul]
- **リスク区分**: [高リスク(決済/BAN) / 中リスク(トリアージ) / 低リスク(UI表示)]
- **入力 State 想定**: 平均 約 XXX tokens (State本文 + Instructions + Criteria)

#### ゲートウェイ適格性判定
- **ハードゲート判定**: [全項目合格 (意味理解・有限空間・即時判断・非生成・証拠充足)]
- **除外条件判定**: [非該当 (決定論・不可逆操作・未知出力に非該当)]
- **適格ステータス**: `Eligible`
- **適合度スコア**: [★3 / ★4 / ★5]
  - 根拠: [月間リクエスト規模、レイテンシ要求、縮退設計の成立可否に基づく評価]

#### 💰 期待TCO試算マトリクス（月間リクエスト規模別）

> **試算パラメータ**:
> - Jev 呼出単価 $C_{\text{jev}}$: $0.042 / 1M tokens (出力無料)
> - フォールバック先: GPT-4o-mini ($0.15 in / $0.60 out per 1M)
> - 想定経路分岐率（記入例。実測値または保守的デフォルトに置き換え、「仮定値」か「実測値」かを明記すること）: $P(\text{auto}) = 93\%$, $P(\text{fallback}) = 5\%$, $P(\text{review}) = 2\%$

| 月間リクエスト規模 | 従来の生成LLMのみ (GPT-4o-mini) | Jev 単体呼出費 | **期待月間TCO (フォールバック・人手込)** | **削減インパクト** |
| :--- | :--- | :--- | :--- | :--- |
| **小規模 (1万回/月)** | 約 $X.XX / 月 | 約 $X.XX / 月 | **約 $X.XX / 月** | **約 XX% 削減** |
| **中規模 (10万回/月)** | 約 $X.XX / 月 | 約 $X.XX / 月 | **約 $X.XX / 月** | **約 XX% 削減** |
| **大規模 (100万回/月)**| 約 $XX.XX / 月 | 約 $XX.XX / 月 | **約 $XX.XX / 月** | **約 XX% 削減** |

#### レイテンシ & 経路別パフォーマンス（加重平均ではなく経路別に提示）

| 実行経路 | 想定レイテンシ (p50 / p95) | 発生頻度 (トラフィック比率) | 備考 |
| :--- | :--- | :--- | :--- |
| **① Jev 高信頼・自動判定** | 80ms / 150ms | 約 93% | PoC前のベンチマーク公称値。実測検証が必要。 |
| **② 中間帯 / 人手レビュー** | 数分 〜 数時間 | 約 2% | 非同期キューに移送。即時応答は縮退値を返却。 |
| **③ 低信頼 / LLMエスカレーション** | 650ms / 1,200ms | 約 5% | GPT-4o-mini による詳細再判定。 |
| **最悪時SLO (タイムアウト)** | **TBD (SLOから逆算)** | - | Jev呼出の絶対締め切り上限。超過時は Safe Default へ即時縮退。 |

#### 評価データ・未確認事項
- **利用可能な評価データセット**: [あり (XX件の正解ラベル) / なし (作成が必要)]
- **主なリスク・未確認事項**: [例: 境界ケースでの不確実性、外部APIダウン時の縮退方針]

---

## 4. 次フェーズ（`jev-logic-architect`）への移行条件
人間によるレビューの上、採用する候補の `decision` を **`approved`** に更新してください。
高リスクな判断については、プロジェクト内に ADR を作成（または `design.md` の Decision 節に起票）した上で `jev-logic-architect` を実行します。
