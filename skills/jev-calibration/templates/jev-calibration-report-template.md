# Jev 較正（Calibration）レポート

## 1. メタ情報
- **Requirement ID**: `[例: REQ-ROUTE-03]`（行番号は用いない）
- **機能名 / 責務**:
- **datasetStatus / datasetVersion**: `frozen` / `[version]`
- **評価セット所在**: [入力で受け取ったパス]
- **architect 初期閾値仮説**: [入力。例: block/allow の仮説値]
- **ゲート基準（VALIDATED 条件）**: [プロジェクト入力への参照。スキルに焼かない]
- **レポート作成日**: [YYYY-MM-DD]

---

## 2. バッチ評価要約
> 実数値・混同行列の詳細表は実験成果物を参照し、ここには要約とポインタを書く。

| 指標 | 結果要約 | 成果物参照 | ゲート対比 |
| :--- | :--- | :--- | :--- |
| 一致率 / 精度系 | | | [達 / 未達 / n/a] |
| 混同行列（要約） | | | |
| 帯域分布（自動 / 中間 / フォールバック） | | | |
| フォールバック率 | | | |
| （任意）較正指標（ECE 等） | | | |

未計測項目は `TBD` と理由を書く。

---

## 3. Dual Threshold / 帯域別検討
### 3.1 仮説と探索範囲
- **探索した閾値候補**: [入力・実験側。固定値をスキルから持ち込まない]
- **コスト行列・優先指標**: [FP/FN 等の重みは入力]

### 3.2 帯域別結果（記入例の列）
| 帯域 | 定義（入力） | 件数比率 | 主要エラー | 備考 |
| :--- | :--- | :--- | :--- | :--- |
| 自動確定（高確信） | | | | |
| 中間（REVIEW） | | | | |
| 自動確定（低確信側 / Allow 側） | | | | |
| Safe Default 発動 | | | | |

---

## 4. 推奨閾値（提案・HITL 確定待ち）
| パラメータ | 推奨値（提案） | 初期仮説との差分 | 確定（HITL） |
| :--- | :--- | :--- | :--- |
| `blockThreshold` 相当 | | | [未確定 / 確定: …] |
| `allowThreshold` 相当 | | | |
| （choice/score）自動確定に使う確率・confidence | | | |

> 推奨は提案である。ポリシーへの注入は HITL 確定後。

---

## 5. validationStatus
- **validationStatus**: `[PENDING | VALIDATED | REJECTED | NEEDS_MORE_DATA]`
- **判定理由**: 
- **HITL 承認者 / 承認日**: ________ / ________（`VALIDATED` 時必須）

### 差し戻し指示（該当時）
| validationStatus | 差し戻し先 | 再開時に必要なこと |
| :--- | :--- | :--- |
| `NEEDS_MORE_DATA` | `jev-eval-set` | [不足層・追加件数方針は入力で再指定] |
| `REJECTED` | `jev-logic-architect` | [問い・criteria・Safe Default の再設計ポイント] |

---

## 6. Shadow レポート契約（記録のみ・本番非適用）
- **shadowStatus**: `[not_started | shadowed]`
- **本番適用の有無**: **なし**（本欄は観測記録のみ。canary / 全量はリリース管理へ委譲）
- **観測期間・対象範囲**: [入力]
- **記録したメトリクス**: [自動率、REVIEW 率、不一致サンプルの所在など]
- **成果物所在**: [入力パス]
- **リリース管理への引き継ぎ事項**: [ゲート達成状況、残リスク、ロールバック観点]

---

## 7. 次フェーズ
- `validationStatus: VALIDATED` かつ必要な shadow 記録が済んだら → **リリース管理プロセス**へ委譲
- それ以外は上記差し戻し先へ戻る
